function [soln] = trapCol(prb)
%TRAPCOL Performs trapezoidal collocation 
%   Detailed explanation goes here

checkPrb(prb);
setDefaults(prb);

fprintf('Performing Trapezoidal collocation with %d collocation points', );
soln = 1;
end

